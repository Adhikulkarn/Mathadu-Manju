Optimize the voiceServer.js latency.

Changes required:

1. Reduce audio chunk window
Replace:
setTimeout(..., 3000)

With:
setTimeout(..., 800)

2. Skip small buffers
Before transcription add:
if (audioBuffer.length < 12000) return

3. Prevent concurrent Whisper calls
Add:
let processing = false

Before transcribeAudio:
if (processing) return
processing = true

After transcription:
processing = false

4. Update whisperSTT.js model

Replace:
whisper-large-v3-turbo

With:
distil-whisper-large-v3-en

Return the updated voiceServer.js and whisperSTT.js.

Start TTS as soon as the agent responds, even if Whisper continues listening.

This makes it feel instant, even if transcription took ~800 ms.